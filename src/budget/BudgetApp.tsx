import App from "@budget/App";

/**
 * Entry point for the Budget Builder module inside MegaHub.
 * Mounted at /budget/* in the parent router.
 */
export default function BudgetApp() {
  return <App />;
}
